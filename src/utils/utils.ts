export function getHelpMessage() {
  return `
  === P2P Chat CLI ===
  Available commands:
  1. list - List all discovered peers
  2. send <peerId> <message> - Send message to a peer
  3. exit - Exit the program
  `
}